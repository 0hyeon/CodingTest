class Node {
  constructor(value) {
    this.value = value;
    this.next = null;
  }
}

class linkedList {
  constructor() {
    this.head = null;
    this.tail = null;
  }
  find(value) {
    let current = this.head;
    while (current.value !== value) {
      //head가 null이 아닌경우 반복문 진행
      //head value랑 다르면 같을때까지 반복
    }
  }
}
